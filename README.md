# PyParser - Simple numeric expression parser and solver which replicates the behavior of eval with minimal usage of built-ins.
Trying to emulate ***eval*** whilst avoiding the usage of **find**, **rfind**, **enumerate** and all the other methods that we take for granted :coffee:
